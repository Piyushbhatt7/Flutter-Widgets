import 'package:flutter/material.dart';

class adding_image extends StatelessWidget {
  const adding_image({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Adding an image'),
        backgroundColor: Colors.amberAccent,
      ),
       
       


    );
  }
}